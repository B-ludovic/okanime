{
  "name": "okanime",
  "version": "1.0.0",
  "description": "Bibliothèque personnelle d'animés en ligne",
  "scripts": {
    "dev": "concurrently --names \"BACK,FRONT\" --prefix-colors \"blue,magenta\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "keywords": ["anime", "library", "nextjs", "express"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}